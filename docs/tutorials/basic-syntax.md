# 基础语法

- 变量：`let x: i32 = 1;`
- 函数：`fn add(a: i32, b: i32) -> i32 { return a + b; }`
- 控制流：`if` / `match` / `while`
- 自定义类型：`struct` 和 `enum`

建议先阅读 `docs/10-language.md`，再运行 `examples/hello_nprt/`。
