fn add(a: i32, b: i32) -> i32 {
  return a + b;
}

fn fib(n: i32) -> i32 {
  if (n <= 1) {
    return n;
  }
  return fib(n - 1) + fib(n - 2);
}

fn main() -> i32 {
  let sum: i32 = add(20, 22);
  let f: i32 = fib(10);
  if (sum == 42) {
    if (f == 55) {
      return 0;
    }
  }
  return 1;
}
